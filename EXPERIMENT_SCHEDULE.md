# VisDrone 实验配置与日程表
## 适配 12GB RTX 3060 | 目标: mAP50-95 从 34.49% 提升到 42-45%

---

## 📊 当前进度

- ✅ **步骤1**: Baseline训练完成 (mAP50-95 = 34.49%)
- ⚠️ **步骤2**: P2训练需重做 (当前29.97%，训练不充分)
- ⏳ **步骤3-12**: 待执行

---

## 🎯 实验总览

| 阶段 | 步骤 | 改进点 | 预期mAP | 训练时间 | Batch | 状态 |
|------|------|--------|---------|----------|-------|------|
| 基础 | 1 | Baseline (YOLOv8s) | 34.49% | 15h | 6 | ✅ |
| 架构 | 2 | +P2 Head (stride=4) | 38-40% | 20h | 4 | ⚠️ 重做 |
| 架构 | 3 | +BiFPN-Lite | 40-42% | 22h | 4 | ⏳ |
| 架构 | 4 | +DCNv2 (可选) | 41-43% | 24h | 3-4 | ⏳ |
| 架构 | 5 | +CARAFE (可选) | 41-43% | 22h | 4 | ⏳ |
| 优化 | 6 | +Head注意力 | +0.5-1.5% | 20h | 4 | ⏳ |
| 优化 | 7 | +损失优化 | +0.5-1.0% | 20h | 4 | ⏳ |
| 优化 | 8 | +小目标增强 | +1.0-2.0% | 22h | 4 | ⏳ |
| 推理 | 9 | Tiling推理 | Recall+3-5% | 2h | - | ⏳ |
| 推理 | 10 | Soft-NMS+TTA | +0.5-1.5% | 3h | - | ⏳ |
| 评估 | 11 | 实验评估 | - | 4h | - | ⏳ |
| 论文 | 12 | 图表制作 | - | 6h | - | ⏳ |

**总预计时间**: 3-4周（包含训练、调试、评估）

---

## 📅 详细实验配置

### 阶段1: 基础准备 ✅

#### 步骤1: Baseline训练 ✅
```bash
python train_baseline.py
```

**配置**:
- 模型: YOLOv8s
- 图像尺寸: 1024×1024
- Batch Size: 6
- Epochs: 300
- 优化器: AdamW (lr=0.002)

**结果**:
- mAP50-95: 34.49%
- mAP50: 53.72%
- 小目标问题: bicycle (16.84%), people (24.57%)

---

### 阶段2: 架构改进

#### 步骤2: P2检测头 ⚠️ **需重新训练**

**问题诊断**:
- ❌ 当前结果: mAP50-95 = 29.97% (下降4.5%)
- ❌ 原因: 只训练了30 epochs (应该300 epochs)
- ✅ 已修复: 更新 `train_p2.py` 配置

**重新训练**:
```bash
python train_p2.py
```

**修正后配置**:
```python
{
    'epochs': 300,          # 修正: 30 → 300
    'batch': 4,             # P2增加计算量，降低batch
    'lr0': 0.002,           # 略微降低学习率
    'warmup_epochs': 15,    # 增加warmup帮助P2层稳定
    'mosaic': 0.9,          # 恢复正常强度
    'close_mosaic': 20,     # 后20 epochs关闭
}
```

**预期结果**:
- mAP50-95: 38-40% (+3.5-5.5%)
- 小目标提升: bicycle +3-5%, people +3-5%
- 训练时间: ~20小时

**验证指标**:
- [ ] mAP50-95 > 38%
- [ ] bicycle AP > 20%
- [ ] people AP > 28%

---

#### 步骤3: BiFPN-Lite集成

**配置文件**: `cfg/models/yolov8s-p2-bifpn.yaml`

**训练脚本**: `train_p2_bifpn.py`

**关键改进**:
- 双向特征融合 (替换PAN)
- 加权特征融合 (learnable weights)
- 保持轻量化 (BiFPN-Lite)

**训练配置**:
```python
{
    'name': 'y8s_p2_bifpn_1024_300ep',
    'epochs': 300,
    'batch': 4,             # BiFPN增加少量计算
    'lr0': 0.002,
    'warmup_epochs': 15,
}
```

**预期结果**:
- mAP50-95: 40-42% (+5.5-7.5%)
- 多尺度目标检测改善
- 训练时间: ~22小时

---

#### 步骤4: DCNv2集成（可选）

**配置文件**: `cfg/models/yolov8s-p2-bifpn-dcn.yaml`

**训练脚本**: `train_p2_bifpn_dcn.py`

**关键改进**:
- Neck后段添加1-2层DCNv2
- 提升形变和遮挡适应性
- 注意: 需要额外依赖

**依赖安装**:
```bash
# 如果需要DCNv2
pip install mmcv-full
# 或使用普通卷积替代（已在代码中处理）
```

**训练配置**:
```python
{
    'name': 'y8s_p2_bifpn_dcn_1024_300ep',
    'epochs': 300,
    'batch': 3-4,           # DCNv2增加显存，可能需要降到3
    'lr0': 0.001,           # 更低学习率，稳定训练
    'warmup_epochs': 15,
}
```

**预期结果**:
- mAP50-95: 41-43% (+6.5-8.5%)
- 遮挡场景改善
- 训练时间: ~24小时

**注意**: 如果显存不足或DCNv2安装困难，可跳过此步骤

---

#### 步骤5: CARAFE上采样（可选）

**配置文件**: `cfg/models/yolov8s-p2-bifpn-carafe.yaml`

**训练脚本**: `train_p2_bifpn_carafe.py`

**关键改进**:
- 内容感知上采样
- 替换关键上采样路径
- 提升边界定位精度

**训练配置**:
```python
{
    'name': 'y8s_p2_bifpn_carafe_1024_300ep',
    'epochs': 300,
    'batch': 4,
    'lr0': 0.002,
}
```

**预期结果**:
- mAP50-95: 41-43%
- 边界IoU提升
- 训练时间: ~22小时

**建议**: DCNv2和CARAFE二选一，或都尝试后对比

---

### 阶段3: 细节优化

#### 步骤6: Head注意力机制

**改进点**:
1. **ECA注意力**: 轻量通道注意力
2. **SimAM注意力**: 无参数空间注意力
3. **分支解耦**: 回归分支加宽 (256→384)

**实现方式**:
- 修改 `yolov8s-p2-bifpn-final.yaml`
- 在Detection Head添加注意力模块

**训练配置**:
```python
{
    'name': 'y8s_p2_bifpn_attention_300ep',
    'epochs': 300,
    'batch': 4,
    'lr0': 0.002,
}
```

**预期提升**: +0.5-1.5%

---

#### 步骤7: 损失函数优化

**改进点**:
1. **IoU损失**: CIoU → SIoU/WIoU (更关注小目标)
2. **分类损失**: BCE → Varifocal Loss (关注难样本)
3. **标签分配**: Task-Aligned Assigner

**实现方式**:
- 修改训练脚本中的损失配置
- 或修改ultralytics源码

**训练配置**:
```python
{
    'box': 7.5,             # 增加box loss权重
    'cls': 0.5,
    'dfl': 1.5,
    # 如果支持自定义损失
    'iou_type': 'siou',     # 或 'wiou'
}
```

**预期提升**: +0.5-1.0%

---

#### 步骤8: 小目标友好增强

**改进点**:
1. **Copy-Paste**: 小目标复制粘贴增强
2. **Mosaic调度**: 后30 epochs关闭
3. **MixUp**: 适度使用
4. **小目标过采样**: 增加小目标样本权重

**训练配置**:
```python
{
    'name': 'y8s_p2_bifpn_final_300ep',
    'epochs': 300,
    'batch': 4,
    'copy_paste': 0.2,      # 关键: 小目标增强
    'mosaic': 0.9,
    'close_mosaic': 30,     # 后30 epochs关闭
    'mixup': 0.1,           # 增加mixup
}
```

**预期提升**: +1.0-2.0% (小目标类别显著提升)

---

### 阶段4: 推理优化

#### 步骤9: Tiling推理

**策略**:
- 1024图像切分为 512×512 tiles (4个)
- 重叠区域: 10-15% (避免边界漏检)
- 结果融合: Soft-NMS或WBF

**实现脚本**: 创建 `inference_tiling.py`

**配置**:
```python
{
    'tile_size': 512,
    'overlap': 0.15,        # 15%重叠
    'conf_threshold': 0.25,
    'iou_threshold': 0.5,
    'fusion_method': 'soft_nms',  # 或 'wbf'
}
```

**预期提升**: Recall +3-5%

---

#### 步骤10: Soft-NMS + TTA

**改进点**:
1. **Soft-NMS**: 替换标准NMS，减少误删
2. **TTA**: 多尺度+翻转测试时增强

**TTA配置**:
```python
scales = [0.9, 1.0, 1.1]    # 3个尺度
flips = [False, True]        # 翻转
# 总共 3×2 = 6次推理
```

**预期提升**: +0.5-1.5%

**注意**: TTA会增加推理时间6倍

---

### 阶段5: 评估与论文

#### 步骤11: 实验评估与可视化

**评估脚本**:
```bash
python compare_results.py
python eval_model.py <model_path>
```

**分析内容**:
1. **分层指标**: 小/中/大目标分别统计
2. **错检分析**: FP/FN分布
3. **PR曲线**: 各类别对比
4. **混淆矩阵**: 类别混淆分析
5. **可视化**: 典型场景检测结果

**输出**:
- 详细评估报告 (Markdown)
- 对比图表 (PNG)
- 可视化结果 (JPG)

---

#### 步骤12: 论文图表与复现

**图表清单**:
1. **表1**: 实验对比表格 (所有模型)
2. **表2**: 消融实验表格 (各改进贡献)
3. **表3**: 各类别AP对比
4. **图1**: PR曲线对比
5. **图2**: 消融实验柱状图
6. **图3**: 可视化检测结果 (4-6张)
7. **图4**: 训练曲线 (loss/mAP)

**复现文档**:
- 环境配置
- 数据准备
- 训练命令
- 评估命令
- 超参数配置

---

## ⚙️ 12GB RTX 3060 显存优化策略

### 显存占用估算

| 配置 | Batch=6 | Batch=4 | Batch=3 | Batch=2 |
|------|---------|---------|---------|---------|
| Baseline | 10GB | 7GB | 5.5GB | 4GB |
| P2 | 11GB | 8GB | 6.5GB | 5GB |
| P2+BiFPN | 12GB | 9GB | 7GB | 5.5GB |
| P2+BiFPN+DCN | OOM | 10-11GB | 8GB | 6GB |

### 优化技巧

**如果遇到OOM**:
```python
# 1. 降低batch size
batch = 3  # 或 2

# 2. 启用梯度累积（等效更大batch）
accumulate = 2  # batch=3, accumulate=2 等效 batch=6

# 3. 降低图像尺寸（最后手段）
imgsz = 960  # 从1024降到960

# 4. 减少workers
workers = 0  # Windows已默认

# 5. 清理缓存
torch.cuda.empty_cache()
```

---

## 📈 预期结果汇总

### 主要指标

| 模型 | mAP50-95 | mAP50 | Recall | 提升 | 训练时间 |
|------|----------|-------|--------|------|----------|
| Baseline | 34.49% | 53.72% | 46.5% | - | 15h |
| P2 | 38-40% | 55-58% | 50-53% | +10-16% | 20h |
| P2+BiFPN | 40-42% | 56-59% | 52-55% | +16-22% | 22h |
| P2+BiFPN+优化 | 42-45% | 58-61% | 54-57% | +22-30% | 24h |

### 小目标类别

| 类别 | Baseline | 目标 | 提升 |
|------|----------|------|------|
| bicycle | 16.84% | 28-35% | +67-108% |
| people | 24.57% | 35-42% | +42-71% |
| pedestrian | 32.21% | 41-47% | +27-46% |

---

## 🚀 下一步行动

### 立即执行（今天）

1. **重新训练P2模型**:
   ```bash
   python train_p2.py
   ```
   - 预计时间: 20小时
   - 监控指标: mAP50-95应 > 38%

2. **监控训练**:
   ```bash
   # 查看训练日志
   tensorboard --logdir runs/visdrone
   ```

### 本周计划

- [ ] 完成P2重训练
- [ ] 评估P2结果
- [ ] 开始BiFPN训练

### 下周计划

- [ ] 完成BiFPN训练
- [ ] 决定是否使用DCNv2/CARAFE
- [ ] 开始细节优化

---

## 📝 注意事项

1. **训练稳定性**: 每次训练保存checkpoint，防止中断
2. **结果记录**: 每次实验后立即记录结果
3. **对比公平性**: 除改进点外，其他配置保持一致
4. **显存监控**: 使用 `nvidia-smi` 监控显存使用
5. **备份权重**: 最佳权重及时备份

---

## 📞 问题排查

### 常见问题

**Q1: 训练中断怎么办？**
```bash
# 从checkpoint恢复
python train_p2.py --resume runs/visdrone/y8s_p2_1024_adamw_300ep/weights/last.pt
```

**Q2: 显存不足？**
- 降低batch size到3或2
- 启用梯度累积
- 检查是否有其他程序占用显存

**Q3: 结果不理想？**
- 检查训练是否收敛
- 查看训练曲线是否正常
- 对比验证集和训练集性能

---

**创建时间**: 2025-11-02
**最后更新**: 2025-11-02
**状态**: P2重训练中

